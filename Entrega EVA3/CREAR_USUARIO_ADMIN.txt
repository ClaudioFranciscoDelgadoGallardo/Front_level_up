╔════════════════════════════════════════════════════════════════╗
║  SOLUCIÓN: CREAR USUARIO ADMIN EN SUPABASE (PASO A PASO)     ║
╚════════════════════════════════════════════════════════════════╝

🎯 PROBLEMA IDENTIFICADO
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Conexión a Supabase: FUNCIONA
❌ Usuario admin@levelup.cl: NO EXISTE en la base de datos

Los logs muestran que la query se ejecutó pero no encontró el usuario.


📋 SOLUCIÓN RÁPIDA (3 MINUTOS)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PASO 1: VERIFICAR SI LA TABLA USUARIOS EXISTE
───────────────────────────────────────────────────────────────
1. Ve a: https://supabase.com/dashboard
2. Selecciona tu proyecto
3. Click en "SQL Editor" (lateral izquierdo)
4. Ejecuta:

SELECT COUNT(*) FROM usuarios;

Si da ERROR "relation usuarios does not exist":
→ La tabla no existe, continúa al PASO 2

Si retorna un número (ej: 0 o 5):
→ La tabla existe, salta al PASO 3


PASO 2: CREAR SCHEMA COMPLETO (SI LA TABLA NO EXISTE)
───────────────────────────────────────────────────────────────
1. En SQL Editor de Supabase
2. Copia y pega TODO el contenido de:
   BD_tablas/schema_completo.sql
3. Click en "Run" o presiona Ctrl+Enter
4. Espera confirmación
5. Continúa al PASO 3


PASO 3: VERIFICAR SI EXISTE EL USUARIO ADMIN
───────────────────────────────────────────────────────────────
En SQL Editor, ejecuta:

SELECT id, correo, rol, activo
FROM usuarios
WHERE correo = 'admin@levelup.cl';

Si retorna 0 filas:
→ El usuario NO existe, continúa al PASO 4

Si retorna 1 fila:
→ El usuario SÍ existe, verifica que activo = true


PASO 4A: CREAR USUARIO ADMIN (OPCIÓN RÁPIDA - RECOMENDADA)
───────────────────────────────────────────────────────────────
Ejecuta este SQL en Supabase SQL Editor:

-- Password real: admin123 (ya hasheado con BCrypt)
INSERT INTO usuarios (
    run,
    nombre,
    apellidos,
    correo,
    password,
    telefono,
    direccion,
    comuna,
    ciudad,
    region,
    fecha_nacimiento,
    rol,
    activo,
    verificado
) VALUES (
    '12345678-9',
    'Admin',
    'Level Up',
    'admin@levelup.cl',
    '$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy',
    '912345678',
    'Av. Providencia 123',
    'Providencia',
    'Santiago',
    'Región Metropolitana',
    '1990-01-01',
    'ADMIN',
    TRUE,
    TRUE
);

Click en "Run"

✅ Credenciales creadas:
   Correo: admin@levelup.cl
   Password: admin123


PASO 4B: CARGAR TODOS LOS DATOS INICIALES (OPCIÓN COMPLETA)
───────────────────────────────────────────────────────────────
Si quieres cargar varios usuarios y datos de prueba:

1. En SQL Editor de Supabase
2. Copia y pega TODO el contenido de:
   BD_tablas/datos_iniciales_completo.sql
3. Click en "Run"
4. Esto creará múltiples usuarios, productos, categorías, etc.


PASO 5: VERIFICAR QUE EL USUARIO FUE CREADO
───────────────────────────────────────────────────────────────
Ejecuta:

SELECT id, correo, rol, activo, verificado
FROM usuarios
WHERE correo = 'admin@levelup.cl';

Debe retornar:
┌────┬───────────────────┬───────┬────────┬────────────┐
│ id │ correo            │ rol   │ activo │ verificado │
├────┼───────────────────┼───────┼────────┼────────────┤
│ 1  │ admin@levelup.cl  │ ADMIN │ true   │ true       │
└────┴───────────────────┴───────┴────────┴────────────┘


PASO 6: PROBAR LOGIN EN POSTMAN
───────────────────────────────────────────────────────────────
Ahora SÍ puedes probar el login:

POST http://localhost:8082/api/usuarios/login

Body (raw JSON):
{
  "correo": "admin@levelup.cl",
  "password": "admin123"
}

Deberías recibir: 200 OK con el token JWT


🔐 CREDENCIALES DISPONIBLES (DESPUÉS DE DATOS INICIALES)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Si ejecutaste datos_iniciales_completo.sql:

┌───────────────────────────┬──────────────┬───────────┐
│ Correo                    │ Password     │ Rol       │
├───────────────────────────┼──────────────┼───────────┤
│ admin@levelup.cl          │ admin123     │ ADMIN     │
│ usuario@test.cl           │ user123      │ CLIENTE   │
│ vendedor@levelup.cl       │ vendedor123  │ VENDEDOR  │
│ juan.perez@email.cl       │ user123      │ CLIENTE   │
│ maria.lopez@email.cl      │ user123      │ CLIENTE   │
└───────────────────────────┴──────────────┴───────────┘

Todas las passwords usan el mismo hash BCrypt en el SQL.


🔍 VERIFICAR PASSWORD HASHEADO
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Si quieres ver la password almacenada:

SELECT correo, password FROM usuarios WHERE correo = 'admin@levelup.cl';

Debe retornar algo como:
password: $2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy

Este hash corresponde a: admin123


❌ SOLUCIÓN DE PROBLEMAS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ERROR: "duplicate key value violates unique constraint"
───────────────────────────────────────────────────────────────
Ya existe un usuario con ese correo.

Solución: Elimínalo primero:
DELETE FROM usuarios WHERE correo = 'admin@levelup.cl';

Luego vuelve a ejecutar el INSERT.


ERROR: "null value in column violates not-null constraint"
───────────────────────────────────────────────────────────────
Falta algún campo requerido.

Solución: Usa el SQL completo del PASO 4A que tiene todos los campos.


ERROR: Usuario existe pero login da 401
───────────────────────────────────────────────────────────────
Posibles causas:
1. Usuario inactivo (activo = false)
   Solución: UPDATE usuarios SET activo = true WHERE correo = 'admin@levelup.cl';

2. Password incorrecta
   Solución: Asegúrate de usar el hash correcto en el INSERT

3. Password en Postman incorrecta
   Solución: Usa exactamente "admin123" (sin espacios)


🧪 PRUEBA COMPLETA PASO A PASO
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. ✅ Verificar conexión (YA FUNCIONA - viste los logs)

2. ✅ Crear usuario admin (ejecutar SQL del PASO 4A)

3. ✅ Verificar creación:
   SELECT * FROM usuarios WHERE correo = 'admin@levelup.cl';

4. ✅ Probar login en Postman:
   POST http://localhost:8082/api/usuarios/login
   Body: {"correo": "admin@levelup.cl", "password": "admin123"}

5. ✅ Si funciona, copiar el token de la respuesta

6. ✅ Usar token en otras peticiones protegidas


📊 VERIFICACIÓN FINAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Ejecuta en Supabase SQL Editor:

-- Ver todos los usuarios
SELECT id, run, nombre, correo, rol, activo FROM usuarios;

-- Contar usuarios
SELECT COUNT(*) as total_usuarios FROM usuarios;

-- Ver solo admins
SELECT * FROM usuarios WHERE rol = 'ADMIN';


🎯 RESUMEN EJECUTIVO
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Conexión a Supabase: ✅ FUNCIONA
2. Servicio Spring Boot: ✅ CORRIENDO
3. Usuario admin: ❌ NO EXISTE (hay que crearlo)

ACCIÓN INMEDIATA:
→ Ve a Supabase SQL Editor
→ Ejecuta el INSERT del PASO 4A
→ Prueba login en Postman
→ Deberías recibir 200 OK con token


═══════════════════════════════════════════════════════════════
¿Sigues con problemas? Comparte:
1. El resultado de: SELECT * FROM usuarios WHERE correo = 'admin@levelup.cl';
2. La respuesta completa de Postman (status + body)
═══════════════════════════════════════════════════════════════

